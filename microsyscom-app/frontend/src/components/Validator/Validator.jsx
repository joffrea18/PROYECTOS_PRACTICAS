import "./Validator.css";

import Footer from "./components/Footer";


// Todo el componente debe de estructurarse así
// + Es un Switch : Cases que contiene
// Las condicionales del if else las cuales a su vez
// guardan mediante la librería del Swal la información del Prompt

const Validator = () => {

// desarrollar este coponente y que muestre la información
// "Ya existe un cliente registrado con ese telefono de contacto"
  return (
    <div className="mainApp">
      <Footer></Footer>
    </div>
  );
}

export default Validator;
